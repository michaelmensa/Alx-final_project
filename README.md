readme for clinicbase
this is nodeJs program. Install Node and npm 
first install the dependencies
  -- run "npm install"
then run "npm run start-server"
you can now run the application on your browser
